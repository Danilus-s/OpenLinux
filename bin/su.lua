local shell = require("shell")
local sh = require("shell")
local perm = require("perm")

local df = perm.getUser(perm.getVar("defuser"))
local root = perm.getUser("root")
perm.setVar("defuser", root[1])
perm.setVar("user", root[1])
os.setenv("HOME", root[4])
sh.execute("/bin/sh.lua")
perm.setVar("defuser", df[1])
perm.setVar("user", df[1])
os.setenv("HOME", df[4] or "/")

dofile("/etc/profile.lua")